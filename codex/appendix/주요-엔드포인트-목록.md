# 부록 B. 주요 엔드포인트 목록

## 1) 커스텀 엔드포인트

### 1-1. custom routes
- `GET /distance`
- `POST /sms`
- `GET /bills`
- `GET /unclaimed-bills`
- `POST /batch-billmonth`
- `POST /batch-finish`
- `POST /send-kakao`
- `GET /unipass`
- `GET /driverinfo`
- `GET /distance-price`

### 1-2. call routes
- `POST /call/match`
- `POST /call/post`

### 1-3. tax routes
- `POST /tax/issue`
- `GET /tax/list`
- `GET /tax/detail`
- `GET /tax/htdetail`
- `POST /webhook`

### 1-4. gift routes
- `GET /gift/balance`
- `POST /gift/send`

### 1-5. dispatch analytics routes
- `GET /dispatch-analytics/trend`
- `GET /dispatch-analytics/share`

## 2) Strapi 기본 리소스(핵심)

다음 리소스는 기본 CRUD 형태로 웹에서 광범위하게 사용됩니다.
- `/orders`
- `/companies`
- `/partners`
- `/items`
- `/prices`
- `/invoices`
- `/invoice-logs`
- `/incoming-taxes`
- `/point-logs`
- `/notifications`
- `/drivers`
- `/giftcards`
- `/quotations`
- `/distance-prices`
- `/change-logs`
- `/users`, `/users/me`, `/users/count`

## 3) 주기 작업(엔드포인트 외)
- `getCallInfo` (5분)
- `updateCallOrder` (3분)
- `autoMatchIncomingTax` (매시 30분)
- `cancelTodayOrders` (매일 23:50, KST)

## 4) 상태/코드 사전

### 4-1. 주문 상태
- 일반: `pending/requested/accounting/withdraw/finished/canceled`
- 선착불: `feeRequested/feeFinished/feeCanceled`

### 4-2. 매입세금계산서 상태
- `pending/automatch/review/approved/declined/canceled`

### 4-3. 자동매칭 실패 사유
- `NO_ORDER`
- `MULTIPLE_ORDERS`
- `TAX_ISSUE`
- `NO_BANK_INFO`

### 4-4. 팝빌 상태 코드
- 100 임시저장
- 300 발행완료
- 301 전송전
- 302 전송대기
- 303 전송중
- 304 전송성공
- 305 전송실패
- 600 발행취소

## 5) 확인 메모
- 웹 경로에 존재하는 일부 API(`/payment-logs`, `/hubs`)는 현재 API 코드베이스에서 직접 확인되지 않음
- 실제 운영 경로 확정 시, 엔드포인트 소유 시스템(현재 API/외부 API/레거시 API) 분류가 필요
